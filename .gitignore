# System files
.DS_Store

# JMeter result artifacts
# results/ (Removed exclusion to allow tracking)

# Sensitive user data — never commit real credentials
# (use data/*.sample.csv as templates instead)
data/usuarios_backend_baseinet_*.csv
